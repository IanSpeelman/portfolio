export type project = {
    title: string,
    description: string,
    image: string,
    github: string,
    demo: string,
    tags: string,
}

export type certificate = {
    title: string,
    description: string,
    image: string,
    certificate: string,
    website: string,
    tags: string,
}
